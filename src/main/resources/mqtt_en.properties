plugin.description=Sends and receives messages via an MQTT broker
mqtt.title=Configure MQTT Broker
mqtt.host.text=Host
mqtt.host.tooltip=Hostname or IP address of the MQTT server
mqtt.port.text=Port
mqtt.port.tooltip=Port used for data transmission (default: 1883)
mqtt.topic.name.text=Topic Name
mqtt.topic.name.tooltip=Name of the MQTT topic used for sending or receiving messages
mqtt.user.text=Username (optional)
mqtt.user.tooltip=Username if authentication is required
mqtt.password.text=Password (optional)
mqtt.password.tooltip=Password if authentication is required
mqtt.use.ssl.label=Connect via HTTPS
mqtt.use.ssl.tooltip=Enable SSL encryption when transmitting data with credentials
mqtt.use.ssl.text=Use SSL
mqtt.required.error=This field must not be empty
